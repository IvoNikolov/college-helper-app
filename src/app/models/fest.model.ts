export class Fest {
    public id: string;
    public name: string;
    public day: string;
    public month: string;

    constructor(id: string, name: string, day: string, month: string) {
        this.id = id;
        this.name = name;
        this.day = day;
        this.month = month;
    }
}